<%= form_with(model: equipamento, local: true) do |form| %>
  <% if equipamento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(equipamento.errors.count, "error") %> prohibited this equipamento from being saved:</h2>

      <ul>
      <% equipamento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, id: :equipamento_nome %>
  </div>

  <div class="field">
    <%= form.label :comando %>
    <%= form.collection_select(:comando_id, @comandos, :id, :nome, :prompt => true) %>
  </div>
  
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
